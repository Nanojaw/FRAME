fn(fib, [fibIdx: unsigned], unsigned) {
    if(lte(fibIdx, 2)) {
        return(1)
    }
    else() {
        return(add(do(fib, sub(fibIdx, 1)),do(fib, sub(fibIdx, 2))))
    }
}

fn(main, unsigned) {
    set([fibHuH: unsigned], do(fib, 8))

    return(0)
}